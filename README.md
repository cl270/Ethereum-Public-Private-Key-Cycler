#Ethereum Address Public Private Key Cycler

This is a Java based command line app that gives the user 100 random Ethereum public private key pairs out of a total of 2^160 and checks to see if the wallet has any Ether in it (in the astronomically unlikely chance that happens).